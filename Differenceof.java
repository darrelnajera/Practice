public class Differenceof {
    public boolean sleepIn(boolean weekday, boolean vacation) {
        //shorten if statement to if boolean is not a weekday or if boolean equals vacation return true
        if (!weekday || vacation) {
          return true;
        }
        //else return false
        return false;
        
      }
}